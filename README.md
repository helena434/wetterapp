# Voraussetzungen

- Node.js 
- npm 

# Installation

## Installieren Sie die Abhängigkeiten:

npm install

## Build-Befehl ausführen, um die Anwendung zu erstellen:

npm run build

## Anwendung starten
serve -s build